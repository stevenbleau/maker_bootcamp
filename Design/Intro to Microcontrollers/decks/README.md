# Deck Organization

This module supports multiple microcontroller decks.

## Structure

- `decks/01-wokwi-pinball/` = Arduino and Wokwi pinball proof of concept

Each deck folder contains its `deck.qmd` slides. Keep simulator source files in
a deck-local `wokwi/` folder so the circuit and lesson stay in sync.